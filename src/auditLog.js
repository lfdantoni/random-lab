export class AuditLog {
  /**
   * @param logType type of log
   * @param subType sub type of log
   * @param data data to log
   */
  log = (logType: string, subType: string, data: string) => { throw 'It is not implemented' }
}