
export class UserService {
  /**
   * @returns an array of users
   */
  users = () => { throw 'It is not implemented' }

  /**
   * @param username 
   */
  register = (username) => { throw 'It is not implemented' }

  /**
   * @param username
   * @returns an User
   */
  find = (username) => { throw 'It is not implemented' }
}